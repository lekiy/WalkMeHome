import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#628b48",
    },
    secondary: {
      main: "#e53935",
    },
    background: {
      default: "#0D324D",
      paper: "#d6d1cd",
    },
    text: {
      primary: "#000000",
    },
  },
});
