import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#7c606b",
    },
    secondary: {
      main: "#e53935",
    },
    background: {
      default: "#D6D1CD",
    },
  },
});
